# /specify

Entry point for stage ① of Spec-Gated Delivery.

**Routes to:** [`../skills/specify/SKILL.md`](../skills/specify/SKILL.md)
**Gate:** `python3 $ADK/scripts/validate_contract.py <contract.json>` &nbsp;(`$ADK` = the kit directory)

Use when starting any new feature, fix, or change. Produces `contract.json` (`status: draft`).
